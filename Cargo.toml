[package]
name = "rspot"
version = "0.1.0"
edition = "2024"

[dependencies]
fuzzy-matcher = "0.3"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
iced = { version = "0.13", features = ["svg", "image"] }
